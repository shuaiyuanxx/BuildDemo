<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <!-- WSLC Container Image Declarations -->
  <ItemGroup>
    <WslcImage Include="echo-server"
               Dockerfile="$(ProjectDir)..\container\server\Dockerfile"
               Context="$(ProjectDir)..\container\server"
               Tag="cs-echo-server:latest"
               Sources="$(ProjectDir)..\container\server" />
    <WslcImage Include="uppercase-worker"
               Dockerfile="$(ProjectDir)..\container\worker\Dockerfile"
               Context="$(ProjectDir)..\container\worker"
               Tag="cs-uppercase-worker:latest"
               Sources="$(ProjectDir)..\container\worker" />
  </ItemGroup>

  <Import Project="$(ProjectDir)..\WslcBuildImage.targets" />

</Project>
